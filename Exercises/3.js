/**
 * Use a for loop (not for...of or for...in) to execute the callback on each element of the array.
 * For every iteration of the loop, pass to the callback as arguments the item itself as the first argument, and the index as the second argument
 * @param {number[]} array
 * @param {Function} callback
 */
function forEach(array, callback) {
  //
}

export default forEach;
